import { Box, Type} from '@mui/material/Box';
import { FC } from 'react';

const index: FC = () => {
  return (
    <Box
      sx={{
        border: '2px solid black',
        flexDirection: 'row',
        height: '100px',
        display: 'flex',
        width: '100%',
      }}
    >
      <Box>

      </Box>
      <Box>

      </Box>
    </Box>
  )
}

export default index
